#!/usr/bin/env bash
set -euo pipefail

parent_path=$(
	cd "$(dirname "${BASH_SOURCE[0]}")"
	pwd -P
)

pushd "$parent_path/.."
sudo nixos-rebuild --flake .# switch --override-input secrets /home/drew/.secrets
